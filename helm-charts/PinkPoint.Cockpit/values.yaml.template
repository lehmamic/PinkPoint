# Default values for PinkPoint.Cockpit.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: docker.pkg.github.com/lehmamic/pinkpoint/cockpit
  tag: #{app-version}
  pullPolicy: Always
  imagePullSecret: githubregcred